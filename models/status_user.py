# from app import db

# status_user = db.Table('status_likes',
#     db.Column('status_id', db.Integer, db.ForeignKey('status.id'), primary_key=True),
#     db.Column('like_id', db.Integer, db.ForeignKey('likes.id'), primary_key=True)
# )